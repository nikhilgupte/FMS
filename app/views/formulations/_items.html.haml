%h3 Ingredients

%table.list.striped
  %thead
    %tr
      %th{ :style => 'width: 70%'} Compound
      %th{ :style => 'text-align: center;', :colspan => 2 } Quantity (grams)
      %th{ :style => 'text-align: center;', :colspan => 2 }== Price (#{current_currency.code})
  %tbody
    - @items.each do |item|
      %tr
        %td= item.compound
        %td.numeric
          = number_with_precision item.quantity, :precision => 3
        %td.numeric.quiet
          == #{number_to_percentage item.quantity_percentage, :precision => 2}
        %td.numeric
          = number_with_precision item.price(current_currency.code), :precision => 2
        %td.numeric.quiet
          == #{number_to_percentage item.price_percentage(current_currency.code), :precision => 2}
